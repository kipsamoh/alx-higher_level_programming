-- script 8
-- script that _displays the _number of records with id = 89 in the
-- table _first_table of the _database hbtn_0c_0 in your _MySQL server.
SELECT COUNT(*) FROM first_table WHERE id = 89;
